<%= form_with(model: brewery, local: true) do |form| %>
  <% if brewery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(brewery.errors.count, "error") %> prohibited this brewery from being saved:</h2>

      <ul>
      <% brewery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<form>
  <div class="form-group row">
   <label class="col-sm-10 col-form-label"> <%= form.label :name %>
   <div class="col-auto">
    <%= form.text_field :name %>
  </div>
  </div>

  <div class="form-group row">
   <label class="col-sm-10 col-form-label"> 
   <%= form.label :year %>
   <div class="col-auto">
    <%= form.number_field :year %>
  </div>
  <div class="form-group row">
 <label class="col-sm-10 col-form-label"> <%= form.label :active %>
  <%= form.check_box :active %>
</div>

 <br/>
   <button type="submit" class="btn btn-primary">Submit</button>
</form>
<% end %>
